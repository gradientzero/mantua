# Source record

- **Item**: `Latent.Space - GPT-6 Astra_ an automated AI Engineer you can hire for _$6 an hour.pdf`
  (preserved exactly as dropped)
- **Title**: "GPT-6 Astra: an automated AI Engineer you can hire for <$6 an hour"
- **Origin**: external — a Latent Space newsletter post
- **URL**: not captured with the item
- **Published**: 2026-09-04, per the item's own dateline
- **Captured**: 2026-09-04 (file mtime)
- **Ingested**: 2026-09-04
- **Provenance**: **captured external material**, processed as `origin: agent` despite arriving
  in `inbox/mine/` — see below. No prose republished. Note published at the owner's request
  (ingest invoked with "publish").

## Provenance: the folder hint was overridden again

Dropped in `inbox/mine/`, which per `inbox/README.md` means "I wrote this." It is not the
owner's writing: the post is written throughout in Latent Space's own first-person plural
("we spent 20B+ tokens...", "we went from prompting humans...") about the outlet's internal
projects, with no first-person voice matching the `origin: human` notes in this notebook and
no frontmatter claiming authorship. This is the fourth item in a row misfiled into
`inbox/mine/` — see `tasks/2026-08-17-third-misfile-into-inbox-mine.md`, updated with this
occurrence rather than opening a new task file for the same open question.

## What was archived

The PDF verbatim, as dropped, no OCR or re-rendering performed (read directly via the native
PDF reader).

## Content note

The piece is promotional in register — a vendor/launch-day writeup with self-reported token
counts, no independent verification, and several redacted screenshots. It is filed and
summarized as such; the wiki note treats its capability and cost claims as unverified rather
than as findings. See the note's "Why it goes in as a draft, not a claim" section (the note
itself was published per the owner's ingest request, but the caveat stands).

## Wiki pages touched

New (`status: published`, `origin: agent`):

- `content/notes/gpt-6-astra-automated-ai-engineer.mdx` — the source note: what the piece
  claims, why it's read skeptically, and what does transfer (the parallelism regime, the
  self-managed run/wave pattern, the harness-is-a-skill-issue framing).

Updated:

- `content/notes/command-over-tokens.mdx` — cross-reference in the parallelism-decision
  section: the piece's 20-50 subagent fleet as a larger-scale instance of the same lever.
- `content/notes/keeping-an-agent-running.mdx` — new closing section noting the piece's
  self-directed run/wave management as a variant of the fourth mechanism merged with the
  third, unverified.
- `content/notes/benchmarking-your-own-agent-spend.mdx` — cross-reference in "Where it thins
  out": the "<$6/hour" figure as a unit price rather than a measured return, contrasted with
  this page's own argument.

## Things deliberately left out

- The specific benchmark numbers claimed for GPT-6 Astra (FrontierMath, ARC-AGI-3) — not
  independently verifiable from this capture and not load-bearing for what the note argues.
- The footnote about parallel work on Grok and Fable-class models, and the "Kill My SaaS"
  competition aside — context for the outlet, not content for the wiki.
