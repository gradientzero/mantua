# Source record

- **Item**: `LLM_Wiki.md`
- **Title**: LLM Wiki — a pattern for building personal knowledge bases using LLMs
- **Origin**: Andrej Karpathy — a plaintext "idea file" meant to be copy-pasted to an LLM
  coding agent. No source URL was included with the drop.
- **Captured**: 2026-07-26
- **Ingested**: 2026-07-26

## A note on this source's dual role

This document isn't an ordinary inbox drop. It's also kept permanently at the repo root
as `LLM_Wiki.md`, referenced directly by `README.md` ("the pattern is ... kept verbatim in
`LLM_Wiki.md`") — the founding pattern mantua.io itself instantiates, not just material to
summarize and file away.

The owner dropped a copy into `/inbox` deliberately, as a second, meta use of the same
material: process it like any other source and give it its own page in the wiki, because
how to use agents this way is itself a topic worth a note here (see
`content/notes/agentic-engineering.mdx`).

Both copies are intentional. The root file is live infrastructure — `README.md` points at
it by that exact path, so it stays there, untouched by ingest. This archived copy exists
so `/sources` stays a complete, consistent record of everything ever ingested, this source
included.

## Wiki pages touched

- `content/notes/llm-wiki-pattern.mdx` (new — `origin: agent`, the pattern's three layers
  and three operations mapped onto this repo's specific choices)
- `content/index/about.mdx` (Lineage section extended with a link to the fuller writeup)
